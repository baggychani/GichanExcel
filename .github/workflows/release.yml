name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: "릴리스 버전 (비워두면 package.json 버전 사용)"
        required: false
        type: string
        default: ""

permissions:
  contents: write

jobs:
  release-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: npm

      - name: Resolve release tag
        id: release
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            INPUT="${{ github.event.inputs.version }}"
            if [ -z "${INPUT// /}" ]; then
              VERSION=$(node -p "require('./package.json').version")
            else
              VERSION="${INPUT#v}"
            fi
            echo "tag=v${VERSION}" >> "$GITHUB_OUTPUT"
            echo "Using release tag: v${VERSION}"
          else
            echo "tag=${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"
          fi

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Install frontend dependencies
        run: npm ci

      - name: Build installer (NSIS Setup)
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ steps.release.outputs.tag }}
          releaseName: "기찬엑셀 ${{ steps.release.outputs.tag }}"
          releaseBody: |
            ## 다운로드

            **Setup (설치형)** — `GichanExcel_*_x64-setup.exe`

            Windows에 설치하고 xlsx / xls / csv 파일을 더블클릭으로 바로 열 수 있습니다.

            ### 설치 방법
            1. `GichanExcel_*_x64-setup.exe` 를 다운로드합니다.
            2. 설치 마법사를 따라 진행합니다.
            3. 시작 메뉴에서 **GichanExcel**을 실행합니다.
          releaseDraft: false
          prerelease: false
          args: --target x86_64-pc-windows-msvc
